### Статутс Github Actions:
[![Actions Status](https://github.com/Raadius/rails-project-65/actions/workflows/hexlet-check.yml/badge.svg)](https://github.com/Raadius/rails-project-65/actions)

### Приложение доступно тут:
https://bulletin-dashboard.onrender.com

---

## ✨ Функционал
- Вход через **GitHub** (OmniAuth, без Devise).
- Объявления (**Bulletin**) с атрибутами: `title`, `description`, `image`, `state`, `category_id`, `user_id`.
- Конечный автомат (**AASM**) для публикации:
    - `draft` → `under_moderation` → `published` / `rejected` → `archived`
- Профиль `/profile`: создание и управление объявлениями (отправка на модерацию, архивирование).
- Главная страница: выводятся **только опубликованные** объявления.
- Панель администратора: просмотр, публикация, отклонение, архивирование объявлений, создание, 
  редактирование и удаление категорий
- Поиск/фильтрация (**Ransack**) + пагинация (**Kaminari**) на главной, в профиле и в админке.
- CI: тесты и линтеры в **GitHub Actions**.

---

## 🛠 Стек
| Слой | Выбор |
|---|---|
| Backend | Ruby **3.2.2**, Rails **7.2.2.2** |
| DB (prod) | PostgreSQL (Render free) |
| DB (dev/test) | SQLite3 |
| JS bundling | esbuild (`-j esbuild`) |
| CSS | Bootstrap 5 (`--css=bootstrap`) |
| Auth | OmniAuth GitHub |
| FSM | AASM |
| Search | Ransack |
| Pagination | Kaminari |
| AuthZ | Pundit |
| CI/CD | GitHub Actions → Render |

---

## ⚡ Быстрый старт локально
> Требования: Ruby 3.2.2, Node ≥ 18, Yarn или npm, SQLite3

```bash
1) Клонирование
git clone https://github.com/Raadius/rails-project-65
cd rails-project-65

2) Подготовка к локальному запуску
запустите make prepare-local
# устнаовит зависимости, 
# сделает миграции, 
# заполнит базу тестовыми данными 
# создаст .env файл для секретов

3) Переменные окружения
Заполните .env:
# GITHUB_CLIENT_ID=...
# GITHUB_CLIENT_SECRET=...
# ROLLBAR_ACCESS_TOKEN опционально, для связи вашего проекта в логгере ошибок Rollbar

4) Запуск
bundle exec rails s
# → http://localhost:3000

5) Запуск тестов
   make run-tests
